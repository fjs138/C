#include <stdio.h> //header for standard input/output library

main() //this is a function, main is where your program begins executing, it expects no arguments, indicated by the empty list ()
{ //statements of main are enclosed in brace {}
	printf("hello, cruel world\n"); //main calls library function printf
	printf("thanks!\n");
}							  // \n represents the newline character
